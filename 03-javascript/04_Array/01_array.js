// Arrays are mutable=> means all the change will be done in a old string
let colors=["red","green","purple","pink"];
console.log(colors);
console.log(colors[0][0]);//first for[index]second for[value for index]

let nums=[1,2,3,4,5];
console.log(nums[3]);

let array=[0.1,1,'a',"aman"];
console.log(array)

let emptyArray=[ ];
console.log(emptyArray);

// Arrays are mutable=> means all the change will be done in a old string
let run=["program","code","idea"];
run[0]="yourProgram";
console.log(run);
